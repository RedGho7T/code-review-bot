spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  ai:
    openai:
      api-key: test

  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false

  task:
    scheduling:
      enabled: false

gitlab:
  api:
    url: http://localhost
    token: test

code-review:
  enabled: false
  rag-enabled: false
  scheduler-enabled: false
  inline-comments-enabled: false
  status-comments-enabled: false
  dry-run: true